# Core: numpy + soundfile for offline. Real-time extras are optional.
numpy>=1.24
soundfile>=0.12
# --- real backends (install as needed) ---
# faster-whisper>=1.0  # streaming multilingual ASR + LID
# edge-tts>=0.14       # 100+ langs streaming TTS, superhuman clarity
# transformers>=4.40 torch>=2.2  # local LLM (Qwen2.5 / Llama-3.2)
# fastapi>=0.110 uvicorn>=0.29  # streaming server
# sounddevice>=0.4  # mic / speaker
# webrtcvad>=2.0 or silero-vad  # robust VAD
